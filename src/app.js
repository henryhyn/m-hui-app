import React, { Component, PropTypes } from 'react';
import ReactDOM from 'react-dom';
import { Page, Button } from 'm-hui';

ReactDOM.render(
    <Page title='HUI Mobile APP' spacing>Hello, world!</Page>,
    document.getElementById('container')
);
